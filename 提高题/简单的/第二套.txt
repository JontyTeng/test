1读取d盘中num.txt(此文件自行创建,内容如下)文件中的内容并存入到List集合中，
 新建一个map集合,map的键为list集合中的元素,值为随机生成的范围1-100的数字，用
两种方法遍历map集合，将map集合中的内容写入到根目录下的sut.txt中


2、一共有100份盒饭,可以在两个窗口领取,假设每次装盒饭的时间为30秒请用线程模拟去饭过程并打印剩余饭盒数量。
	
3、	
	  2、按照以下需求实现相关代码
	  		a、创建一个Student类,属性有(name,age,score)分别生成set/get方法,以及有参无参构造,以便可以在创建对象
	  		      时直接给该三个属性进行显示赋值。
	 		b、控制台提示用户输入5次相关数据,输入方式如下(姓名,年龄,分数)
	 		c、将用户输入的这5组数据根据逗号进行拆分,然后创建5个Student对象并对他们进行显示初始化,并且根据成绩的降序将
	 		   学生的姓名打印出来.
	 